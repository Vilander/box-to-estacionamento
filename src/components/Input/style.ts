import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  input: {
    width: '100%',         // Faz o campo ocupar todo o espaço que a Home der para ele
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    paddingHorizontal: 12,
    height: 50,            // Altura do campo
    fontSize: 16,
    backgroundColor: '#fff',
  },
});